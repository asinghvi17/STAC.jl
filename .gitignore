Manifest.toml
/docs/build/
/docs/Manifest.toml
.DS_Store

# DocumenterVitepress manages the npm side of the docs build
/docs/node_modules/
/docs/package-lock.json

# Agent scratch space: task artifacts live outside the repo (symlink), local config is per-machine
/.humanlayer/tasks/
/.humanlayer/workspace.local.json
/CLAUDE.local.md
/.claude/settings.local.json
